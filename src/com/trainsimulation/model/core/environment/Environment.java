package com.trainsimulation.model.core.environment;

import com.trainsimulation.model.simulator.SimulationObject;

// A blue print for simulation objects which are meant to serve the passengers (all simulation objects which are not
// passengers
public interface Environment extends SimulationObject {
}
